export enum ProviderStatus {
  INACTIVE,
  ACTIVE,
}

export const OFFSET = 0;

export const LIMIT = 10;

export const REDIS_SET_EXPIRED_TIME = 1000;

export enum Sort {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum HTTPMethod {
  POST = 'post',
  GET = 'get',
  PUT = 'put',
  DELETE = 'delete',
}
